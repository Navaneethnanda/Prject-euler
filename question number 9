# equation by which we can represent pythogorean triplets is when a^2+b^2=c^2
# a=m^2-n^2
# b=2mn
# c=m^2+n^2

# by altering m and n values all possible abc values can be found when m>n




for n in range(40):
    for m in range(n+1,40):
        a=m**2-n**2
        b=2*m*n
        c=m**2+n**2
        if  a+b+c==1000:
            print(a*b*c)
            exit()
